import { Container, Scope } from 'typescript-ioc';
import DatabaseConnection from '../src/loaders/databaseLoader'

describe('Proceso fake Test', () => {
    it('should test that true === true', () => {
      expect(true).toBe(true)
    })
  })